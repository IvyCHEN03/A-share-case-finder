---
name: a-share-corner-case-finder
description: 检索并核验A股IPO、再融资及并购重组中的非标准、边界性和监管口径不明案例。用于用户提出“找案例”“有没有先例”“偏门案例”“corner case”“审核口径”“是否符合发行条件”，或要求按阈值、事项发生时点、主体身份、项目状态筛选A股案例时。先核实现行规则，再以交易所、证监会及原始披露文件形成结论；二手材料仅用于发现线索。
---

# A股偏门案例检索

面向投行和 PE 项目组，输出可追溯、可复核的案例研究。不得替代律师意见、保荐机构内核意见或正式监管沟通。

## 遵守三条铁律

1. 只用交易所、证监会及原始披露文件支持最终事实结论。将搜索摘要、数据库标签、公众号和律所文章仅作为线索。
2. 先核实现行有效监管规则，再搜索案例。记录文件名、条款、发布/生效日期和规则性质。
3. 区分“暂未发现”和“不存在”。未完成足够覆盖的全样本检索时，使用审慎表述。

## 执行工作流

### 0. 核实现行规则

- 查找现行有效的规则原文，避免用历史版本判断新案例。
- 判断规则属于“允许但需论证”“原则允许且设豁免”“明确禁止”中的哪一类。
- 按规则维度拆分子类型；不得将受不同规则约束的事项混为一类。

### 1. 定义检索口径

用不超过 8 行确认研究问题、严格口径、宽口径、板块、时间范围、项目状态、关键阈值/计算时点和排除项。需要完整口径单时读取 `assets/case-request-template.md`。

若用户未指定时间范围，默认覆盖注册制以来，并回溯核准制典型案例；以上市项目为主，终止/撤回项目单列。

### 2. 建立概念树和检索词

构造对象词、事项词、时点词、同义词及反向排除词。遇到常见事项时读取 `references/case-type-library.md`；遇到新类型时沿用其中结构，但仍需另做必要条件测试。

### 3. 高召回获取候选

- 优先扫描招股书和问询回复中的标准化强制披露字段。
- 多轮检索，并用知名案例公司名反查以检查覆盖度。
- 将匿名文章、数据库标签和二手盘点仅登记为 D 类线索。
- 若结果高度集中于单一来源，继续扩展检索。

### 4. 锁定原始文件

按 `references/source-priority.md` 选择信源，并按 `references/entry-points.md` 定位交易所、证监会和巨潮原始文件。核对申报稿、上会稿、注册稿等版本及披露日期，并通读相关轮次问询。

### 5. 精确核验

记录文件名、版本、披露日期、章节、页码、关键事实和链接。严格区分：

- 文件已定位；
- 已实际读取相关条款原文。

仅定位文件不得写“已核实”。无法读取 PDF、扫描件或页码时，如实说明障碍。不得根据搜索摘要推定原文，不得虚构公司、页码、条款、日期或监管结论。

### 6. 必要条件测试与分层

- A：严格符合，且已读取一手条款原文。
- B：高度接近但缺一项关键条件，或文件已定位但条款原文未读取。
- C：形似而实质不符；写明排除理由。
- D：仅有二手线索，未完成一手核验。

对阈值问题反向检查边界值。区分个案安排与普遍监管口径。对终止/撤回项目单独判断事项是否为终止主因；无证据时写“无法判断”。

### 7. 交付

按 `assets/case-output-template.md` 输出：结论先行，然后列示覆盖范围、监管规则、严格案例、近似案例及排除理由、重点案例卡片、盲区、置信度和可用于内部 Memo 的审慎表述。

## 维护案例库

需要复用历史结果时读取 `references/verified-cases.md`。复用前重新检查规则、项目状态、文件链接和版本是否变化。除非用户明确要求写入案例库，否则只在交付中提出建议条目，不修改该文件。

## 交付前检查

- 已先核实现行规则并拆分子类型。
- 已明确严格与宽口径。
- 检索轮次与主题复杂度匹配，且未依赖单一二手来源。
- 已检查披露文件版本、日期、事项时点和数字口径。
- 至少对优先案例完成原始条款核验，或明确标记未完成。
- 已单列近似案例、终止项目、D 类线索和检索盲区。
- 已注明检索截止日、一手出处和置信度。

## 输出风格

结论优先、措辞审慎、可直接用于投行/PE内部 Memo。宁可少而准。明确区分事实、推断与判断。对争议事项同时给出严格口径和宽口径结论。
